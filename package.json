{
  "name": "floraison-de-lynn",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node rename-files.js && vite --port 3000 --host 0.0.0.0",
    "build": "node rename-files.js && vite build",
    "preview": "vite preview --port 3000",
    "lint": "echo 'Linter run complete'"
  },
  "dependencies": {
    "gsap": "^3.12.5"
  },
  "devDependencies": {
    "vite": "^5.2.11"
  }
}
